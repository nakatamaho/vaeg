# RECNN report: title

Status: NOT_STARTED / IN_PROGRESS / FAIL / BLOCKED / IMPLEMENTED_UNVERIFIED /
WAITING_HUMAN / PASS. Select exactly one and explain it.

Feature ID and mapped legal M ID:
Assigned branch:
Base SHA:
Evaluated source SHA:
Evaluated binary/toolchain identity:
Evidence-only SHA, if applicable:
Task/spec version:

## Scope and changes

State the exact objective, changed files and why each changed. Identify deliberate
unchanged contracts: Native + OSD, Displayed final client, PCM/guest time, QA and
screenshots, C core boundary, private-data and history restrictions.

## Implementation findings and decisions

Give actual call sites and verified behavior, not assumptions. Record approved
source/format/timing adjustments and why they do not silently change the user scope.

## Checks

| Check ID | Exact command/environment | Exit code / outcome | Evidence |
|---|---|---|---|
| Not run | No command yet | NOT_RUN | None |

Separate synthetic fixture, source integration, target compile, real backend
runtime, VLC playback and human gate. Note missing requirements explicitly.
Do not reproduce private asset names/paths/hashes or raw private outputs.

## Integrity results

Record applicable expected/actual frame and sample counts, normalized RGB/PCM
comparisons, PTS quanta and alignment error, common start/end cut, queue high-water
mark, fault outcomes and normal-file/partial-file disposition. Omit nonapplicable
fields with explanation rather than inventing numbers.

## Review and limitations

Record lock/ownership/arithmetic/source-scope review, recording-OFF regression,
actual supported platforms, unresolved issues and concrete blocked prerequisites.

## Gate and next task

Machine gate:
Human gate: NOT_REQUIRED / PENDING / explicit accepted statement and date.
Push/remote state under repository policy:
Next dependency-ready REC task and host requirement:

Stop here; do not implement the next task in this report session.
